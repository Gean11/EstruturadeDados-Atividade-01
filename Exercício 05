
#include <stdio.h>

int main() {
    int vetor[] = {5, 15, 25, 40, 50};
    int tamanho = sizeof(vetor) / sizeof(vetor[0]);

    printf("Vetor de tras para frente:\n");

    for (int i = tamanho - 1; i >= 0; i--) {
        printf("%d ", vetor[i]);
    }

    printf("\n");

    return 0;
}
